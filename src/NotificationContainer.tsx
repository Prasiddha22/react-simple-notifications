import React from 'react';
type Props = {
  enterTimeout: number;
  leaveTimeout: number;
};

export const NotificationContainer = ({
  enterTimeout = 400,
  leaveTimeout = 400,
}: Props) => {
  return <></>;
};
